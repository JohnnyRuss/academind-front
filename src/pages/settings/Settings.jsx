import { Content, SettingsContainer, SideBar } from "../../components/Settings";

function Settings() {
  return (
    <SettingsContainer>
      <SideBar />
      <Content />
    </SettingsContainer>
  );
}

export default Settings;
